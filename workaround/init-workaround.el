;;; init-workaround.el --- Workarounds for Emacs Unsolved Bugs  -*- lexical-binding: t; -*-

;;; Commentary:
;; Load all workaround source files in the directory.

(require 'fix-theme)
(require 'fix-pgtk)

(provide 'init-workaround)

;;; init-workaround.el ends here
